<% content_for :title, "New featured item" %>

<% form_for(@featured_item, :html => {:multipart => true}) do |f| %>
  <%= f.error_messages %>

  <dl>
    <dt>
      <%= f.label :title %>
    </dt>
    <dd>
      <%= f.text_field :title %>
    </dd>
    <dt>
      <%= f.label :content %>
    </dt>
    <dd>
      <%= f.text_area :content %>
    </dd>
    <dt>
      <%= f.label :image %>
    </dt>
    <dd>
      <%= f.file_field :image %>
    </dd>    
  </dl>
  
  <%= f.submit 'Create' %>
  
<% end %>

<%= link_to 'Back', featured_items_path %>